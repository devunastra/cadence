# Dependencies
node_modules/

# Next.js
.next/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# OS
.DS_Store
Thumbs.db

# Superpowers brainstorm sessions (temp files)
.superpowers/
tsconfig.tsbuildinfo

# Claude / MCP
mcp.json
.mcp.json
.claude/mcp.json

# Env files with secrets
env.txt

# Local Netlify folder
.netlify

# Deno lock (generated by Netlify Edge Functions)
deno.lock

# Notion reconciliation audit output (contains lead PII)
.notion-audit/

# Retell flow audit dumps (contain live tool auth tokens)
.audit-*.json

# Retell agent snapshots — agent.json / conversation-flow-*.json embed the live
# custom_tool Bearer tokens for the n8n webhooks, same exposure as .audit-*.json
backups/
